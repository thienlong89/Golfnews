import { StyleSheet } from 'react-native';

export default Style = StyleSheet.create({
    container: {
        flex: 1
    },

    container_content : {
        flex: 1,
        backgroundColor: '#fff'
    }
});